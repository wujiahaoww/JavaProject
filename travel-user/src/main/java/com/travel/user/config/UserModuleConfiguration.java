package com.travel.user.config;

import org.springframework.context.annotation.Configuration;

/**
 * 用户模块装配入口（占位：可在此注册 user 域 Bean、AOP、属性绑定等）。
 */
@Configuration
public class UserModuleConfiguration {
}
