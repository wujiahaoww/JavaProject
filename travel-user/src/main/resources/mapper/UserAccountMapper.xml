<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.travel.user.infrastructure.mapper.UserAccountMapper">

    <resultMap id="UserAccountMap" type="com.travel.domain.user.UserAccount">
        <id property="id" column="id"/>
        <result property="openId" column="open_id"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="nickname" column="nickname"/>
        <result property="avatarUrl" column="avatar_url"/>
    </resultMap>

    <sql id="BaseColumns">id, open_id, phone, email, password_hash, nickname, avatar_url</sql>

    <select id="findByOpenId" resultMap="UserAccountMap">
        SELECT <include refid="BaseColumns"/> FROM user_account WHERE open_id = #{openId} LIMIT 1
    </select>

    <select id="findByPhone" resultMap="UserAccountMap">
        SELECT <include refid="BaseColumns"/> FROM user_account WHERE phone = #{phone} LIMIT 1
    </select>

    <select id="findByEmail" resultMap="UserAccountMap">
        SELECT <include refid="BaseColumns"/> FROM user_account WHERE email = #{email} LIMIT 1
    </select>

    <select id="findById" resultMap="UserAccountMap">
        SELECT <include refid="BaseColumns"/> FROM user_account WHERE id = #{id} LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO user_account (open_id, phone, email, password_hash, nickname, avatar_url)
        VALUES (#{openId}, #{phone}, #{email}, #{passwordHash}, #{nickname}, #{avatarUrl})
    </insert>

    <update id="updateProfile">
        UPDATE user_account
        SET nickname = #{nickname},
            avatar_url = #{avatarUrl},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>
</mapper>
